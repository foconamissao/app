import {sb} from './supabase.js';
export async function loadQuestoes(ctx){if(ctx.user.demo)return;const {data}=await sb.from('questoes').select('id,enunciado,banca,ano,disciplinas(nome),assuntos(nome)').eq('ativo',true).limit(30);const el=document.querySelector('#questionList');el.innerHTML=(data||[]).map(q=>`<div class="task"><span><b>${q.disciplinas?.nome||''} · ${q.assuntos?.nome||''}</b><small>${q.banca||'Banca não informada'} ${q.ano||''}</small><div>${q.enunciado.slice(0,180)}${q.enunciado.length>180?'…':''}</div></span></div>`).join('')||'<div class="empty">Nenhuma questão cadastrada.</div>'}
