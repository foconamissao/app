import {requireAuth,getProfile,signOut,avatarUrl} from './auth.js';
const labels={dashboard:'Início',missao:'Minha Missão',cronograma:'Cronograma',questoes:'Questões',flashcards:'Flashcards',erros:'Caderno de Erros',simulados:'Simulados',desempenho:'Desempenho',ranking:'Ranking',admin:'Administração'};
function initials(name=''){return name.trim().split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()||'FM'}
function mountAccount(profile){const top=document.querySelector('.topbar');if(!top)return;const old=document.querySelector('#userName');old?.remove();const wrap=document.createElement('div');wrap.className='account-wrap';const img=profile.avatar_path?`<img src="${avatarUrl(profile.avatar_path)}" alt="">`:`<span>${initials(profile.nome)}</span>`;wrap.innerHTML=`<div class="account-avatar">${img}</div><b class="account-name">${profile.nome||'Participante'}</b><button class="account-gear" type="button" aria-label="Opções da conta">⚙</button><div class="account-menu hidden"><a href="./admin.html#perfil" class="profile-link">👤 Editar perfil</a><button type="button" class="logout-menu">🚪 Sair</button></div>`;top.appendChild(wrap);const gear=wrap.querySelector('.account-gear'),menu=wrap.querySelector('.account-menu');gear.onclick=()=>menu.classList.toggle('hidden');wrap.querySelector('.logout-menu').onclick=signOut;document.addEventListener('click',e=>{if(!wrap.contains(e.target))menu.classList.add('hidden')});if(profile.role!=='admin')wrap.querySelector('.profile-link').href='./perfil.html';}
export async function boot(page){const user=await requireAuth();if(!user)return null;const profile=await getProfile(user);if(!profile){document.body.innerHTML='<main class="login-shell"><section class="login-card"><h1>Cadastro não encontrado</h1><p>Não foi possível carregar seu cadastro.</p><a class="btn" href="./login.html">VOLTAR</a></section></main>';return null}if(profile.status!=='aprovado'||!profile.ativo){await signOut();return null}document.querySelector('#pageTitle')?.replaceChildren(document.createTextNode(labels[page]||'Missão PE'));document.querySelector(`[data-nav="${page}"]`)?.classList.add('active');if(profile.role==='admin')document.querySelectorAll('[data-admin]').forEach(el=>el.classList.remove('hidden'));else if(page==='admin'){location.href='./index.html';return null}mountAccount(profile);document.querySelector('#logout')?.addEventListener('click',signOut);const side=document.querySelector('.sidebar'),overlay=document.querySelector('.overlay');document.querySelector('#mobileMenu')?.addEventListener('click',()=>{side?.classList.toggle('open');overlay?.classList.toggle('open')});overlay?.addEventListener('click',()=>{side?.classList.remove('open');overlay?.classList.remove('open')});return{user,profile}}
