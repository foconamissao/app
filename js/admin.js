import {sb} from './supabase.js';
export function setupAdmin(ctx){if(ctx.profile?.role!=='admin'){document.querySelector('#adminArea').innerHTML='<div class="empty">Acesso restrito.</div>';return}document.querySelector('#questionForm')?.addEventListener('submit',async e=>{e.preventDefault();if(!sb)return alert('Configure o Supabase primeiro.');const f=new FormData(e.currentTarget),payload=Object.fromEntries(f.entries());payload.ativo=true;const {error}=await sb.from('questoes').insert(payload);if(error)return alert(error.message);e.currentTarget.reset();alert('Questão cadastrada.');});}
